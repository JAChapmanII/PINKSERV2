$bot.ignore list of nicks to ignore

help? Hmm.... Default help function? Special function in function namespace:
	!func.help?
	!help => !eval "!echo $1.help"

$bot.quiet (bool of quiet or not)
quiet => $bot.quiet = true; !in 60m $bot.quiet = false;
unquiet => $bot.quiet = false;

kick => !irc KICK $1 $2
op => !irc MODE +o $1

