#ifndef MODULES_HPP
#define MODULES_HPP

#include <map>
#include <string>

namespace modules {
	//extern std::map<std::string, Function *> map;
	// TODO: all of this...

	bool init(std::string fileName);
	bool deinit(std::string fileName);
}

#endif // MODULES_HPP
