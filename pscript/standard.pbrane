:!on text (null => ($text =~ ':o/|\\o:')? !or 'o/' '\o')
:!on text (null => ($toUs && $text =~ ":<3:")? !echo "<3")
:!on text (null => ($toUs && $text =~ ":</3:")? !echo ":(")

:chance => (!drand 0 1) < $1
:!on join (null => (!or "Hello" "Hi") + ", " + $nick + "!")

o/
\o
:$toUs = true
<3
</3

